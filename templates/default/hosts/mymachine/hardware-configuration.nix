# Placeholder — replace this whole file with your machine's hardware scan:
#
#   sudo nixos-generate-config --show-hardware-config \
#     > hosts/mymachine/hardware-configuration.nix
#
throw "Replace hosts/mymachine/hardware-configuration.nix with the output of `sudo nixos-generate-config --show-hardware-config`."
